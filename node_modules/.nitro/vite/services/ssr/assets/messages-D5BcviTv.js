import { t as supabase } from "./supabase-CFPQp7Vx.js";
import { i as useAuth } from "./router-T3nTODM3.js";
import { i as AvatarImage, n as Avatar, r as AvatarFallback, t as SiteHeader } from "./site-header-BwfxK3TA.js";
import { t as useRequireAuth } from "./use-require-auth-C335W4BK.js";
import { useEffect, useState } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { formatDistanceToNow } from "date-fns";
//#region src/routes/messages.tsx?tsr-split=component
function MessagesInbox() {
	const { loading } = useRequireAuth();
	const { profile } = useAuth();
	const [conversations, setConversations] = useState([]);
	const [loadingList, setLoadingList] = useState(true);
	useEffect(() => {
		if (loading || !profile) return;
		const load = async () => {
			setLoadingList(true);
			const { data } = await supabase.from("messages").select("*").or(`sender_id.eq.${profile.id},recipient_id.eq.${profile.id}`).order("created_at", { ascending: false });
			if (!data) {
				setLoadingList(false);
				return;
			}
			const byPartner = /* @__PURE__ */ new Map();
			for (const m of data) {
				const partnerId = m.sender_id === profile.id ? m.recipient_id : m.sender_id;
				if (!byPartner.has(partnerId)) byPartner.set(partnerId, {
					content: m.content,
					at: m.created_at,
					unread: m.recipient_id === profile.id && !m.read_at
				});
			}
			const partnerIds = Array.from(byPartner.keys());
			if (partnerIds.length === 0) {
				setConversations([]);
				setLoadingList(false);
				return;
			}
			const { data: profiles } = await supabase.from("profiles").select("id, username, display_name, avatar_url").in("id", partnerIds);
			const list = (profiles ?? []).map((p) => {
				const info = byPartner.get(p.id);
				return {
					partner: p,
					lastContent: info.content,
					lastAt: info.at,
					unread: info.unread
				};
			});
			list.sort((a, b) => new Date(b.lastAt).getTime() - new Date(a.lastAt).getTime());
			setConversations(list);
			setLoadingList(false);
		};
		load();
	}, [loading, profile]);
	if (loading) return null;
	return /* @__PURE__ */ jsxs("div", {
		className: "min-h-screen bg-background text-foreground",
		children: [/* @__PURE__ */ jsx(SiteHeader, {}), /* @__PURE__ */ jsxs("main", {
			className: "mx-auto max-w-2xl px-6 py-14",
			children: [
				/* @__PURE__ */ jsx("h1", {
					className: "text-4xl leading-tight",
					children: "Messages"
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-2 text-muted-foreground",
					children: "Private conversations. Find someone on the People page to start."
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "mt-10 divide-y divide-border border-y border-border",
					children: [
						loadingList && /* @__PURE__ */ jsx("p", {
							className: "py-8 text-sm text-muted-foreground",
							children: "Loading…"
						}),
						!loadingList && conversations.length === 0 && /* @__PURE__ */ jsx("p", {
							className: "py-8 text-sm text-muted-foreground",
							children: "No messages yet."
						}),
						conversations.map((c) => /* @__PURE__ */ jsxs(Link, {
							to: "/messages/$username",
							params: { username: c.partner.username },
							className: "flex items-center gap-4 py-5 transition-colors hover:bg-accent/40",
							children: [
								/* @__PURE__ */ jsxs(Avatar, {
									className: "size-11",
									children: [/* @__PURE__ */ jsx(AvatarImage, { src: c.partner.avatar_url ?? void 0 }), /* @__PURE__ */ jsx(AvatarFallback, { children: (c.partner.display_name || c.partner.username).slice(0, 1).toUpperCase() })]
								}),
								/* @__PURE__ */ jsxs("div", {
									className: "min-w-0 flex-1",
									children: [/* @__PURE__ */ jsx("p", {
										className: "font-medium",
										children: c.partner.display_name || c.partner.username
									}), /* @__PURE__ */ jsx("p", {
										className: "truncate text-sm text-muted-foreground",
										children: c.lastContent
									})]
								}),
								/* @__PURE__ */ jsxs("div", {
									className: "flex flex-col items-end gap-1",
									children: [/* @__PURE__ */ jsx("span", {
										className: "text-xs text-muted-foreground",
										children: formatDistanceToNow(new Date(c.lastAt), { addSuffix: true })
									}), c.unread && /* @__PURE__ */ jsx("span", { className: "size-2 rounded-full bg-brass" })]
								})
							]
						}, c.partner.id))
					]
				})
			]
		})]
	});
}
//#endregion
export { MessagesInbox as component };
