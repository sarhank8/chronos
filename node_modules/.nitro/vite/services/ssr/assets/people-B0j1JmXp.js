import { t as supabase } from "./supabase-CFPQp7Vx.js";
import { i as useAuth } from "./router-T3nTODM3.js";
import { t as Button } from "./button-DRsC1qZi.js";
import { i as AvatarImage, n as Avatar, r as AvatarFallback, t as SiteHeader } from "./site-header-BwfxK3TA.js";
import { t as useRequireAuth } from "./use-require-auth-C335W4BK.js";
import { t as Input } from "./input-DicJzR9-.js";
import { useEffect, useState } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { Check, Clock3, Lock, Search, UserPlus } from "lucide-react";
//#region src/routes/people.tsx?tsr-split=component
function PeoplePage() {
	const { loading } = useRequireAuth();
	const { profile } = useAuth();
	const [query, setQuery] = useState("");
	const [people, setPeople] = useState([]);
	const [followMap, setFollowMap] = useState({});
	const [searching, setSearching] = useState(true);
	const loadPeople = async (q) => {
		setSearching(true);
		let request = supabase.from("profiles").select("*").order("created_at", { ascending: false }).limit(30);
		if (q.trim()) request = request.or(`username.ilike.%${q}%,display_name.ilike.%${q}%`);
		if (profile) request = request.neq("id", profile.id);
		const { data } = await request;
		setPeople(data ?? []);
		if (profile && data && data.length > 0) {
			const { data: follows } = await supabase.from("follows").select("following_id, status").eq("follower_id", profile.id).in("following_id", data.map((p) => p.id));
			const map = {};
			follows?.forEach((f) => {
				map[f.following_id] = f.status;
			});
			setFollowMap(map);
		}
		setSearching(false);
	};
	useEffect(() => {
		if (!loading) loadPeople(query);
	}, [loading]);
	const handleFollow = async (target) => {
		if (!profile) return;
		setFollowMap((m) => ({
			...m,
			[target.id]: target.is_private ? "pending" : "accepted"
		}));
		const { error } = await supabase.from("follows").insert({
			follower_id: profile.id,
			following_id: target.id
		});
		if (error) setFollowMap((m) => {
			const next = { ...m };
			delete next[target.id];
			return next;
		});
	};
	const handleUnfollow = async (target) => {
		if (!profile) return;
		setFollowMap((m) => {
			const next = { ...m };
			delete next[target.id];
			return next;
		});
		await supabase.from("follows").delete().eq("follower_id", profile.id).eq("following_id", target.id);
	};
	if (loading) return null;
	return /* @__PURE__ */ jsxs("div", {
		className: "min-h-screen bg-background text-foreground",
		children: [/* @__PURE__ */ jsx(SiteHeader, {}), /* @__PURE__ */ jsxs("main", {
			className: "mx-auto max-w-3xl px-6 py-14",
			children: [
				/* @__PURE__ */ jsx("h1", {
					className: "text-4xl leading-tight",
					children: "People"
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-2 text-muted-foreground",
					children: "Find people to follow."
				}),
				/* @__PURE__ */ jsxs("form", {
					onSubmit: (e) => {
						e.preventDefault();
						loadPeople(query);
					},
					className: "relative mt-8",
					children: [/* @__PURE__ */ jsx(Search, { className: "absolute top-1/2 left-4 size-4 -translate-y-1/2 text-muted-foreground" }), /* @__PURE__ */ jsx(Input, {
						value: query,
						onChange: (e) => setQuery(e.target.value),
						placeholder: "Search by username or name",
						className: "rounded-full py-5 pl-11"
					})]
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "mt-8 divide-y divide-border border-y border-border",
					children: [
						searching && /* @__PURE__ */ jsx("p", {
							className: "py-8 text-sm text-muted-foreground",
							children: "Searching…"
						}),
						!searching && people.length === 0 && /* @__PURE__ */ jsx("p", {
							className: "py-8 text-sm text-muted-foreground",
							children: "No one found."
						}),
						people.map((person) => {
							const status = followMap[person.id];
							return /* @__PURE__ */ jsxs("div", {
								className: "flex items-center gap-4 py-5",
								children: [
									/* @__PURE__ */ jsxs(Avatar, {
										className: "size-11",
										children: [/* @__PURE__ */ jsx(AvatarImage, { src: person.avatar_url ?? void 0 }), /* @__PURE__ */ jsx(AvatarFallback, { children: (person.display_name || person.username).slice(0, 1).toUpperCase() })]
									}),
									/* @__PURE__ */ jsxs("div", {
										className: "min-w-0 flex-1",
										children: [/* @__PURE__ */ jsxs(Link, {
											to: "/profile/$username",
											params: { username: person.username },
											className: "flex items-center gap-1.5 font-medium hover:underline",
											children: [person.display_name || person.username, person.is_private && /* @__PURE__ */ jsx(Lock, {
												className: "size-3.5 text-muted-foreground",
												strokeWidth: 1.5
											})]
										}), /* @__PURE__ */ jsxs("p", {
											className: "truncate text-sm text-muted-foreground",
											children: ["@", person.username]
										})]
									}),
									status === "accepted" ? /* @__PURE__ */ jsxs(Button, {
										variant: "outline",
										size: "sm",
										className: "rounded-full",
										onClick: () => handleUnfollow(person),
										children: [/* @__PURE__ */ jsx(Check, {
											className: "size-4",
											strokeWidth: 1.75
										}), " Following"]
									}) : status === "pending" ? /* @__PURE__ */ jsxs(Button, {
										variant: "outline",
										size: "sm",
										className: "rounded-full text-muted-foreground",
										onClick: () => handleUnfollow(person),
										children: [/* @__PURE__ */ jsx(Clock3, {
											className: "size-4",
											strokeWidth: 1.75
										}), " Requested"]
									}) : /* @__PURE__ */ jsxs(Button, {
										size: "sm",
										className: "rounded-full",
										onClick: () => handleFollow(person),
										children: [/* @__PURE__ */ jsx(UserPlus, {
											className: "size-4",
											strokeWidth: 1.75
										}), " Follow"]
									})
								]
							}, person.id);
						})
					]
				})
			]
		})]
	});
}
//#endregion
export { PeoplePage as component };
